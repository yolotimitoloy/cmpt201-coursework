#include <stdio.h>
#include <unistd.h>

int main(){
  int pid = fork();
  if (pid==fork()){
    for (int i = 0; i<20; i++){
      printf("sleeping\n");
      sleep(1);
    }
  } else { 
    for (int i = 0; i<20; i++){
      printf("sleeping\n");
      sleep(2);
    }
  }
  printf("done\n");
}

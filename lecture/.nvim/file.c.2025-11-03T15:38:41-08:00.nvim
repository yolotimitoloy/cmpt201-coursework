#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <unistd.h>
int main(){

  int fd = open("tmp", O_CREAT);
  
  write(fd, "Hello World\n", 12);
  lseek(fd, 6, SEEK_CUR);
   
  close(fd);
}

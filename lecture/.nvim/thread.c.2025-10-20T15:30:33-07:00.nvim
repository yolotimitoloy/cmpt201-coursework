#include <pthread.h>
#include <stdint.h>
#include <stdio.h>
#include <unistd.h>
void *printing(int i) {
  sleep(i);
  printf("HELP ME!\n");
  sleep(i);
  pthread_exit(NULL);
}

int main() {
  for (int i = 0; i < 5; i++) {
   pthread_t temp;
   pthread_create(&temp, NULL, printing(1), NULL);
  }
}

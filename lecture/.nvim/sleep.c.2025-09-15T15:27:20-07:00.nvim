#include <stdio.h>
#include <unistd.h>

int main() {
  //int pid = fork();
  
  //if (pid == fork()) {
    for (int i = 0; i < 5; i++) {
      printf("sleeping1\n");
      sleep(1);
    }
    printf("parent done.\n");
  //} else {
    fork();
    for (int i = 0; i < 5; i++) {
      printf("sleeping2\n");
      sleep(2);
    }
    printf("child done.\n");
  //}
  printf("process done.\n");
}
